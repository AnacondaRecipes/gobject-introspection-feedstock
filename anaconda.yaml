anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: GNOME/gobject-introspection
      use_max_tag: true
      include_regex: ^\d+\.\d*[02468]\.\d+$
    packages:
      - g-ir-build-tools
      - g-ir-host-tools
      - gobject-introspection
      - libgirepository
